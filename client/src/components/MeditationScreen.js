import React from 'react';
import { View, Text } from 'react-native';

const MeditationScreen = ({route}) => {
    console.log(route)
  return (
    <View >
      {/* Add content for the specific meditation type here */}
    </View>
  );
};

export default MeditationScreen;